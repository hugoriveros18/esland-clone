import { Inter, Onest, Jura } from 'next/font/google'

export const inter = Inter({ subsets: ['latin'] });
export const onest = Onest({ subsets: ['latin'] });
export const jura = Jura({ subsets: ['latin'] });